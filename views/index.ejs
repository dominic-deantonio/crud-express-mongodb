<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <title>Star Wars Quotes</title>
</head>

<body>
    <div class='d-flex flex-column align-items-center'>
        <h1 class='container text-center'> May Node and Express be with you </h1>
        <form class='d-flex flex-row' action="/quotes" method="POST">
            <input id='name' class='m-1' type="text" placeholder="Name" name="name">
            <input id='quote' class='m-1' type="text" placeholder="Quote" name="quote">
            <button class='btn btn-primary m-1' type="submit">Submit</button>
        </form>
        <h2> Quotes </h2>
        <div class="container d-flex flex-wrap justify-content-center">
            <!-- Loop through quotes -->
            <% for(var i=0; i < quotes.length; i++) {%>
                <div class="card m-1" style="width: 18rem;">
                    <!-- Output quote from the iterated quote object -->
                    <span class='card-body'>
                        <strong>
                            <%= quotes[i].name %>
                        </strong>:
                        <%= quotes[i].quote %>
                    </span>
                </div>
                <% } %>

           
        </div>
        <div class='d-flex flex-column justify-content-center align-items-center'>
            <button class='btn btn-primary m-1' onClick='clickMe();'>Replace Yoda's Quote</button>
            
            <h2 class='text-center mt-5'>Remove Darth Vader!</h2>
            <p>
                Delete one Darth Vader's quote. Does nothing if there are no more Darth
                Vader's quote
            </p>
            <button onClick='deleteVader();' class='btn btn-danger' id="delete-button">Delete Darth Vader's quote</button>        
            <div id='no-quote-to-delete-alert' class='alert alert-danger m-2' hidden>There are no Darth Vader quotes to delete</div>
        </div>

    </div>
    <script src="/main.js"></script>

</body>

</html>